#2 b) no da resultado ya que no se encuentra en el rango que impusimos inicialmente
#3) la funcion es sort
#4) si, generan la misma matriz
#5) salta error porq para multiplicar matrices seria a%*%b 
 te pone el vector que antes estaba en columna, en fila ya que se traspuso. 